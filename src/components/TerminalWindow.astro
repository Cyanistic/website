---

---

<style>
	div.outer{
		position: fixed;
		width: calc(100vw - 22px);
		height: calc(100% - 98px);
		overflow-x: hidden;
		overflow-y: auto;
		display: flex;
		flex-direction: column;
		background-color: #1a1b26CC;
		margin-top: 5px;
		margin-bottom: 5px;
		border-color: var(--default);
		border-width: 3px;
		border-style: solid;
		border-radius: 20px;
	}
</style>
<div class="outer" transition:animate="slide">
	<slot/>
</div>
